[
  {"query": "we want everyone in the firm to be able to watch and read everything on vendorco.invalid but nobody should be able to create, upload or edit anything there. enforcement is our secure web gateway and it only matches domains. i have a HAR of a viewer session. what rules do i push and what breaks?", "should_trigger": true},
  {"query": "following on from the host set you gave me - gateway logs show 41k allowed requests yesterday to media-edge-eu2.vendorco.invalid, which is not in your list. what do i tell the change board on thursday?", "should_trigger": true},
  {"query": "attached is view.har and register.har from learnportal.invalid. i need to know which hosts are required just to view content vs which ones are only there for authoring, and whether any host does both. domain-level blocking only, thats all the gateway can do", "should_trigger": true},
  {"query": "legal wants me to send a network capture to an outside firm for their analysis. its a .har from our internal app at app.acmeinternal.invalid and it definitely has session cookies and customer records in the query strings. how do i strip it down so its still useful to them", "should_trigger": true},
  {"query": "our DLP engine moved from PCRE to RE2 and about a dozen of our detection patterns wont compile any more, mostly the ones using lookbehind and backreferences. can you work out what each one was actually trying to match and give me an RE2 equivalent, or tell me where there isnt one", "should_trigger": true},
  {"query": "is there a documented enterprise policy that stops users installing extensions from the store but still allows the four we push ourselves? need it per browser and per OS, and i need to know which browser version the policy landed in because half our estate is two majors behind", "should_trigger": true},
  {"query": "static-cdn.sharedco.invalid serves both the player js for the bit we want people to use and the uploader widget for the bit we dont. if i block it viewing dies, if i allow it uploads stay possible. what are my actual options here", "should_trigger": true},
  {"query": "management asked for something simple: people can look at stuff on the supplier portal, they just cant put anything into it. i have no idea whether thats even possible with what we have. can you work out what we'd need and whether the answer is yes or no", "should_trigger": true},
  {"query": "the player bundle on meetingco.invalid references svc-bridge.meetingco.invalid with /v2/render and /v2/ingest paths but my capture never calls it. can you work out whether that host is a read path, a write path or both, and what evidence would settle it", "should_trigger": true},
  {"query": "we run an old build of an embedded web component and theres a CVE out for it. i need to know whether the vulnerable code path is even reachable in how we deploy it, what an attacker would actually get, and what we can do this week that isnt an upgrade", "should_trigger": true},
  {"query": "i grabbed a har of our checkout page because its taking 6 seconds to load. can you work out which requests are blocking render and where the waterfall stalls? mostly interested in the images and the third party tag manager", "should_trigger": false},
  {"query": "getting 'blocked by CORS policy' in the console when my react dev server on localhost:5173 calls my local api on :8080. i've tried adding the header in the express app but its still failing on the preflight", "should_trigger": false},
  {"query": "can you write unittest cases for parse_inventory.py? it reads a json file, flattens the nested items array and writes a csv. i want coverage for the empty file case and the malformed json case", "should_trigger": false},
  {"query": "we're moving mail for ourcompany.invalid to a new provider and our stuff keeps landing in spam. i think the spf record is wrong and we never set up dkim. can you give me the exact dns records to publish and the order to do it in so we dont break current sending", "should_trigger": false},
  {"query": "a prospect sent over a 200 question vendor security questionnaire, mostly SOC2 and encryption-at-rest type stuff. i have our answers from last year in a spreadsheet. can you draft responses for the new questions and flag the ones where our answer changed", "should_trigger": false},
  {"query": "half the sales team says their bookmarks and saved passwords vanished after the laptop refresh. they were signed into the browser with their work account. is there a way to get the profile back from the old machine image", "should_trigger": false},
  {"query": "need a grafana dashboard showing p95 latency and error rate per endpoint for our public api, data is in prometheus, and an alert when p95 goes over 800ms for 5 minutes", "should_trigger": false},
  {"query": "we need to open postgres 5432 from the app subnet 10.40.2.0/24 to the db subnet 10.40.9.0/24 for the new reporting job. can you write the terraform for the security group rule and the change ticket description", "should_trigger": false},
  {"query": "we're picking an MDM for about 400 macs and 150 windows laptops. can you put together a comparison of the main options on price, DEP support and how painful the migration is from what we have now", "should_trigger": false},
  {"query": "draft the customer-facing email about last week's outage. keep it short, apologetic but not grovelling, mention the root cause was a config change and that we've added a second approval step", "should_trigger": false}
]
